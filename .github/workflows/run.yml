name: 每小时自动部署 Back4App

on:
	schedule:
		- cron: "0 * * * *"  # 每小时整点执行
	workflow_dispatch:     # 允许手动触发

jobs:
	run:
		runs-on: ubuntu-latest
		steps:
			- name: 拉取仓库代码
			  uses: actions/checkout@v4

			- name: 运行自动部署脚本
			  run: python auto_deploy.py